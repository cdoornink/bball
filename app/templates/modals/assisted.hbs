{{#modal-dialog}}
  <div class="shot-modal">
    <div class="title">
      Assisted by
    </div>
    <div class="option-row">
      {{#if controllers.game.leftPlayerSelected}}
        {{modal-card-row colors=controllers.game.model.left team=controllers.game.leftOnCourt action="submit" exclude=controllers.game.selectedPlayer}}
      {{else}}
        {{modal-card-row colors=controllers.game.model.right team=controllers.game.rightOnCourt action="submit" exclude=controllers.game.selectedPlayer}}
      {{/if}}
    </div>
    <div class="confirmation-buttons">
      <div class="cancel spread">
        <button class="cancel-button" {{action 'cancel'}}>No Assist</button>
      </div>
    </div>
  </div>
{{/modal-dialog}}