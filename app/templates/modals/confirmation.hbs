{{#modal-dialog clickOverlay="cancel"}}
  {{model.header}}
  {{model.message}}
  {{#if model.continue}}
    <div class="confirmation-buttons">
      <div class="cancel">
        <button class="cancel-button" {{action 'cancel'}}>Cancel</button>
      </div>
      <div class="continue">
        <button class="continue-button" {{action 'continue'}}>End Game</button>
      </div>
    </div>
  {{/if}}
{{/modal-dialog}}
